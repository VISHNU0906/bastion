apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    # Pinned uid so the provisioned dashboards bind to this datasource on load.
    uid: bastion-prometheus
    jsonData:
      timeInterval: 15s
